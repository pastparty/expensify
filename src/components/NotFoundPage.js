import React from 'react';
import { Link } from 'react-router-dom';

const NotFoundPage = () => (
    <div>
        OH NOEZ 404 <Link to="/">Back to home</Link>
    </div>
);

export default NotFoundPage;
